---
	import QuoteContainer from '../atoms/QuoteContainer.svelte';
	import Quote from '../atoms/Quote.svelte';
	import Quoter from '../atoms/Quoter.svelte';

	const { quote, quoter } = Astro.props;
---

<QuoteContainer>
	<Quote>{quote}</Quote>
	<Quoter>{quoter}</Quoter>
</QuoteContainer>
